# Dependencies
node_modules/

# CDK synthesis output (build artifact, regenerated by `cdk synth`)
cdk.out/
cdk.context.json

# TypeScript build output — the app runs via tsx and Lambdas bundle at deploy time,
# so compiled output should never be committed.
*.js
*.d.ts
!jest.config.js
dist/
coverage/

# Credentials and local overrides
.env
.env.*
*.pem

# Scratch / OS
scratch
.DS_Store
